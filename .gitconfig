[credential]
  helper = osxkeychain
[user]
  name = Rafael Kendrik
  email = rafaelkendrik@gmail.com
[push]
  followTags = true
# [core]
  # editor = code --wait
[alias]
  s = !git status -s
  a = !git add -i && git commit --amend
  c = !git add -i && git commit -m
  l = !git log --pretty=format:'%C(blue)%h %C(white)%s - %C(cyan)%cn, %C(green)%cr %C(red)%d'
  lo = !git log --oneline